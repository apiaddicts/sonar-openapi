swagger: "1.2"
info:
  version: 1.0.0
  title: Swagger Petstore
paths:
  /pets/{petId}:
    get:
      responses:
        '200':
          description: success
          schema:
            type: array
            items:
              $ref: '#/definitions/Pet'
  /pets/1234:
    get:
      responses:
        '200':
          description: success
          schema:
            $ref: '#/definitions/Pet'
definitions:
  Pet:
    type: object
