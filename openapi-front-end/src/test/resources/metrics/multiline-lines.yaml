openapi: "3.0.1"
x-no-sonar: SomeId
info:
  version: 1.0.0
  title: Swagger Petstore
paths:
  /pets:
    # a comment line
    x-sonar-disable: SomeId
    get:
      responses:
        x-sonar-enable: SomeId
        '200':
          description: |
            some multiline
            description
  #
  # another comment line
  /newpets:
    $ref: '#/paths/~1pets'
